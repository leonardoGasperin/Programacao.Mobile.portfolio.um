import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Programacao.Mobile.portfolio.um',
  webDir: 'dist'
};

export default config;
